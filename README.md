# 基于 AST 图传播与会话记忆的 SQL 安全审计

当前项目是五层概念架构，其中第一层暂未设计：

1. Layer 1: SQL 输入层
   - 当前仅占位，不展开
2. Layer 2: AST 层
   - 负责 SQL 解析与血缘图构建
3. Layer 3: 风险图谱层
   - 负责风险初始化与传播
4. Layer 4: 记忆层
   - 负责跨查询污点记忆
5. Layer 5: 解释层
   - 负责确定性矩阵与可选 LLM 结构化解释
